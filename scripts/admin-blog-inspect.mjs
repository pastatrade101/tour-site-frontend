import { chromium } from 'playwright';
const browser=await chromium.connectOverCDP('http://127.0.0.1:9223');const page=browser.contexts()[0].pages()[0];page.setDefaultTimeout(20000);
await page.goto('https://goldfinch.makutano.co.tz/admin/blog',{waitUntil:'networkidle'});console.log((await page.locator('main').innerText()).slice(0,16000));console.log('LINKS',await page.locator('a').evaluateAll(as=>as.map(a=>({text:a.innerText.trim(),href:a.href})).filter(a=>/edit/i.test(a.text))));console.log('BUTTONS',await page.locator('button').evaluateAll(bs=>bs.map(b=>b.innerText.trim()).filter(Boolean).slice(-80)));
const edit=page.getByRole('button',{name:'Edit',exact:true});if(await edit.count()){await edit.first().click();await page.waitForTimeout(700);console.log('FORMTEXT',(await page.locator('body').innerText()).slice(-14000));console.log('FIELDS',await page.locator('input,textarea,select,[contenteditable="true"]').evaluateAll(fs=>fs.map(f=>({name:f.name,aria:f.getAttribute('aria-label'),type:f.type,value:f.value??f.innerText,placeholder:f.placeholder,checked:f.checked}))));console.log('FORMBUTTONS',await page.locator('button').evaluateAll(bs=>bs.map(b=>b.innerText.trim()).filter(Boolean).slice(-100)));}
await browser.close();
