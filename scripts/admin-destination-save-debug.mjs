import { chromium } from 'playwright';const browser=await chromium.connectOverCDP('http://127.0.0.1:9223');const page=browser.contexts()[0].pages()[0];page.setDefaultTimeout(30000);await page.goto('https://goldfinch.makutano.co.tz/admin/destinations',{waitUntil:'networkidle'});const rows=page.locator('tbody tr');const names=await rows.evaluateAll(ns=>ns.map(n=>n.querySelector('td')?.innerText.split('\n')[0].trim()));await rows.nth(names.indexOf('Zanzibar')).getByRole('button',{name:'Edit',exact:true}).click();await page.locator('[name=location]').fill('Zanzibar Archipelago, Indian Ocean');const events=[];page.on('response',r=>{if(/destination|api|supabase/i.test(r.url()))events.push({status:r.status(),method:r.request().method(),url:r.url()})});await page.getByRole('button',{name:'Save Changes',exact:true}).last().click();await page.waitForTimeout(5000);console.log('EVENTS',events);console.log('ALERTS',await page.locator('[role=alert]').allTextContents());console.log('TEXT',(await page.locator('body').innerText()).slice(-2500));await browser.close();
