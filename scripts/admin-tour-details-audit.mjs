import { chromium } from 'playwright';
const browser=await chromium.connectOverCDP('http://127.0.0.1:9223');const page=browser.contexts()[0].pages()[0];page.setDefaultTimeout(60000);
await page.goto('https://goldfinch.makutano.co.tz/admin/tour-details',{waitUntil:'networkidle'});const select=page.locator('select[name=tour_id]');const tours=await select.locator('option').evaluateAll(os=>os.map(o=>({value:o.value,label:o.textContent.trim()})).filter(o=>o.value));for(const t of tours){await page.locator('select[name=tour_id]').selectOption(t.value);await page.waitForTimeout(1800);const texts=await page.locator('button').allTextContents();const inc=texts.find(x=>/^Inclusions\s*\d+/.test(x.trim()))?.trim();const exc=texts.find(x=>/^Exclusions\s*\d+/.test(x.trim()))?.trim();console.log(JSON.stringify({tour:t.label,inclusions:inc,exclusions:exc}));}await browser.close();
