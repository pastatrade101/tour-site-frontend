import{chromium}from'playwright';const b=await chromium.connectOverCDP('http://127.0.0.1:9223');const p=b.contexts()[0].pages()[0];console.log(await p.locator('input,textarea,select').evaluateAll(fs=>fs.filter(f=>!f.checkValidity()).map(f=>({name:f.name,type:f.type,value:f.value,message:f.validationMessage,required:f.required}))));console.log((await p.locator('body').innerText()).slice(-1800));await b.close();
